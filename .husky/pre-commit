# Run linting on staged files
npm run precommit

# Check for console.log and debugger statements
git diff --cached --name-only --diff-filter=ACM | grep -E '\.(ts|tsx)$' | xargs grep -l "console.log\|debugger" && echo "⚠️  Remove console.log/debugger statements" && exit 1 || exit 0

